/**
 * an interface used to pass on the capacity method that should return number of cells.
 */
public interface HashSetCapacity {

    /**
     * checks number of cells
     * @return total number of the hashset array
     */
    int capacity();
}
